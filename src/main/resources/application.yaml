spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

#mybatis:
#  type-aliases-package: com.kyy.carbackend.domain
#  mapper-locations: mappers/**/*.xml


logging:
  level:
    org:
      mybatis: TRACE